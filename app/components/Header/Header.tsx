import Image from "next/image";
import Link from "next/link";

export default function Header() {
  

  return (
    <header className="flex items-center justify-center w-full h-[44px] bg-[#ffffff] border-[0.1px] border-[rgba(0,0,0,0.25)]">
      <span>이디저디</span>
    </header>
  );
};